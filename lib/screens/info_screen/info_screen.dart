import 'package:flutter/material.dart';

class InfoScreen extends StatelessWidget {
  const InfoScreen({super.key});
  static const path = '/info_screen';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
